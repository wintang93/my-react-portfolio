REACT_APP_CHATBOT_PROXY_URL=https://ask-sherwin-proxy.sherwin-portfolio.workers.dev
# REACT_APP_CHATBOT_PROXY_URL=http://localhost:8787

# Analytics dashboard admin-reveal key. Do NOT put the real value here —
# this file is committed. Set the real key in .env.local (gitignored).
# NOTE: REACT_APP_* is baked into the public JS bundle, so this only gates
# the convenience reveal of the reset control. Enforce reset server-side.
# REACT_APP_ANALYTICS_ADMIN_KEY=changeme